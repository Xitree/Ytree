<button
  id="back-to-top-btn"
  class="back-to-top-loli hide"
  type="button"
  aria-label="返回顶部"
  title="返回顶部"
  onclick="backToTop()"
>
  <img src="/images/back-to-top.gif" alt="" width="70" height="70" draggable="false" />
</button>

<script is:raw is:inline>
  function backToTop() {
    window.scroll({ top: 0, behavior: "smooth" });
  }
</script>

<style>
  .back-to-top-loli {
    display: block;
    width: 70px;
    height: 70px;
    padding: 0;
    margin: 0;
    border: none;
    background: transparent;
    box-shadow: none;
    cursor: pointer;
    pointer-events: auto;
    flex-shrink: 0;
    line-height: 0;
    appearance: none;
    -webkit-appearance: none;
    overflow: visible;
    transition:
      transform 0.3s ease,
      opacity 0.3s cubic-bezier(0.4, 0, 0.2, 1),
      width 0.3s cubic-bezier(0.4, 0, 0.2, 1),
      height 0.3s cubic-bezier(0.4, 0, 0.2, 1);
  }

  .back-to-top-loli img {
    width: 100%;
    height: 100%;
    object-fit: contain;
    pointer-events: none;
    user-select: none;
    -webkit-user-drag: none;
  }

  .back-to-top-loli:hover {
    transform: translateY(-4px);
  }

  .back-to-top-loli:active {
    transform: translateY(0) scale(0.96);
  }

  .back-to-top-loli:focus-visible {
    outline: 2px solid var(--primary);
    outline-offset: 2px;
  }

  .back-to-top-loli.hide {
    transform: scale(0.5);
    opacity: 0;
    pointer-events: none;
    width: 0;
    height: 0;
    overflow: hidden;
  }

  @media (max-width: 768px) {
    .back-to-top-loli {
      width: 60px;
      height: 60px;
    }
  }
</style>
